use "stdlib/io.tc"
use "stdlib/conv.tc"
/*
=>-> just means char*[], a slice (array) of pointers to u8
*/
i8 fn main: =>->u8 args { 
    if(args.len == 0) {
        print("no input")
    }
    if(args.ptr[1] == "help"){
        print("usage: args <command>\n args: add, sub, mul, div")
    }

}